{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Game",
            "type": "debugpy",
            "request": "launch",
            "module": "my_game",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}